# Optional dependencies, not needed by CI or the test suite.
-r requirements.txt
# Local chart generation in 01_data_prep.py (CI takes the reconcile-only path
# when the registration export is absent, so it never imports these):
matplotlib==3.11.1
seaborn==0.13.2
# PDF whitepaper generator (manual tool: model_whitepaper.py):
reportlab==5.0.0
# Hotel room-block audit list generator (manual tool: hotel_audit.py);
# also in requirements-dev.txt because its test suite exercises the
# workbook writer:
openpyxl==3.1.5
# Markup-drift monitor (structure_monitor.py); Phase 3 rewrites this to
# requests+bs4 so selenium can be dropped:
selenium==4.48.0
