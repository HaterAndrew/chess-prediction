# Runtime dependencies for the daily prediction pipeline (auto_update.py chain,
# scrapers, model). Pinned to the versions the current test suite passes on so
# a PyPI release cannot silently change model output on the next cron run (G2).
# Bump deliberately via a PR that re-runs the test gate.
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
requests==2.33.0
urllib3==2.7.0
beautifulsoup4==4.12.3
zstandard==0.25.0
